
package src;

import java.util.List;

import Grafos.Grafo;
import Grafos.Vertice;

public class Dijkstra<V, A> {
    public Integer[] minDistPonderada(Grafo<V, A> g, Vertice<V> v) {
//		List<Vertice<V>> vs = g.vertices();
//		
//		Integer[] dists = new Integer[vs.size()];
//	
//		// Inicializar distancias
//		for (int i = 0; i < dists.length; i++) {
//		    dists[i] = vs[i].equals(v) ? 0 : Integer.MAX_VALUE;
//		}
//	
//		FilaPrioridades fp = new FilaPrioridades(vs, dists);
//		while (!fp.isEmpty()) {
//		    Vertice u = fp.removeMin();
//		    for (Vertice adj : G.adjacentes(u)) {
//			if (relaxar(adj, u, dists)) {
//			    fp.minimizar(adj /* completar... */);
//			}
//		    }
//		}
	
		return null;
    }
}
